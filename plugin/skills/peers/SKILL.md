---
name: peers
description: List locally authenticated Research Peer identities and endpoints. Use only when the local owner explicitly invokes /research-peer:peers.
disable-model-invocation: true
---

# List peers

Run `research-peer peer list`. Show user name, shortened identity and TLS fingerprints, endpoint, and allow state. Never print certificates, private keys, invite tokens, or credentials.
